import React from 'react';
import { Container, Typography } from '@mui/material';

export default function ProfileContent () {

    return (
        
        <Container component={"main"} maxWidth="xs">
            <Typography>Este es el perfil</Typography>
        </Container>

    )

}